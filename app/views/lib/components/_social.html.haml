// Social

%nav.social
  %ul.social-box
    - magensinus_networks.each do |item|
      %li.social-item
        = link_to image_tag(item.thumb_box_url.to_s, class: "left"), item.url, class: "link left"
